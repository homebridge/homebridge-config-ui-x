name: "CodeQL"

on:
  push:
    branches: [ main, beta* ]
  pull_request:
    branches: [ main, beta* ]
  schedule:
    - cron: '17 9 * * 2'

jobs:
  analyze:
    uses: homebridge/.github/.github/workflows/codeql-analysis.yml@main
